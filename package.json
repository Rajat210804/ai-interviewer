{
  "name": "ai-interviewer",
  "version": "1.0.0",
  "private": true,
  "description": "AI virtual interviewer that runs realistic interviews from your CV and a job description",
  "scripts": {
    "build": "npm ci --prefix backend --omit=dev && npm ci --prefix frontend --include=dev && npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "test": "npm test --prefix backend"
  },
  "engines": {
    "node": ">=22.12"
  }
}
